name: Prompt Security Scan (Deprecated)

# Deprecated workflow retained for historical context.
# The previous implementation referenced a non-existent `dcyfr-ai-agents/` path in this repository,
# causing deterministic setup/upload failures on every scheduled run.
#
# TODO: Replace with the current security scanning stack when migration plan is finalized.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deprecated
        run: |
          echo "This workflow has been deprecated to prevent false CI failures."
          echo "Please use security-suite.yml and other active security workflows instead."